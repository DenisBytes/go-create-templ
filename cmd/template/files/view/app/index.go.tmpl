package app

import (
    "{{.ProjectName}}/view/layout"
)

templ Index() {
    @layout.App(false) {
        <div class="w-full max-w-screen-xl mx-auto text-center py-12">
            <h1 class="font-black text-5xl leading-[1,2] mb-2">Hello from the App page</h1>
            <h2 class="text-xl font-semibold mb-2">this is some application</h2>
        </div>
    }
}
